#!/bin/sh

# This file provides command alias to make easy the workflow within the development of this project
# Note that the alias are ready to be executed from within the root path of the project

# Activates the Python virtual environment. Note that the ZDC development team
# typically has a .venv on the root of the project and we typically are using an
# Unix like terminal (git bash or a WSL2 environment)
alias venv_up_win='source ./.venv/Scripts/activate'
